# Python Learning

A learning repository — code I write as I learn Python. The end goal is building Telegram bots.

## Structure

- `week_02_data_files/` — file I/O and JSON: exercises plus a console expense calculator that saves data between runs.
- `week_03_oop/` — first steps with OOP: the expense tracker rewritten using classes and the @dataclass decorator.

> Week 1 is not in this repo — I started saving my code from week 2 onwards.

---

_Python 3.x, standard library only (so far)._
